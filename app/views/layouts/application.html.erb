<!DOCTYPE html>
<html>
  <head>
    <title>Cafe</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
     <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.4.1/css/bootstrap.min.css" integrity="sha384-Vkoo8x4CGsO3+Hhxv8T/Q5PaXtkKtu6ug5TOeNV6gBiFeWPGFN9MuhOf23Q9Ifjh" crossorigin="anonymous">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.13.0/css/all.min.css" />

    <%= stylesheet_link_tag 'application', media: 'all' %>
    <%= javascript_pack_tag 'application' %>
  </head>

  <body>
  <nav class="navbar navbar-expand-lg navbar-light bg-light">
      <a class="navbar-brand" href="#">Cafetaria</a>
      <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNavAltMarkup" aria-controls="navbarNavAltMarkup" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNavAltMarkup">
        <ul class="navbar-nav mr-auto mt-2 mt-lg-0">
          <%= link_to 'Home', root_path, class: "nav-item nav-link" %>
          <%= link_to 'New Menu', new_daily_menu_path, class: "nav-item nav-link" %>
          <%= link_to 'All Menus', daily_menus_path, class: "nav-item nav-link" %>
        </ul>
        <% if user_signed_in? %>
          <a class="nav-item nav-link">Hello, <%= current_user.name %><a>
          <%= link_to 'Sign Out', session_path, method: :delete, class: 'nav-item nav-link'%>
        <% else %>
          <%= link_to 'Sign In', new_session_path, class: 'btn btn-outline-success my-2 my-sm-0'%>
        <% end %>
      </div>
    </nav>
    <div class="container-fluid">
      <% if flash[:notice].present? || flash[:alert].present? %>
        <div class="flash-message">
          <%= flash[:notice] || flash[:alert] %>
        </div>
        <% end %>
      <%= yield %>
    </div>
  </body>
</html>
